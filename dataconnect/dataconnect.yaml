specVersion: "v1beta"
serviceId: "agendize"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "agendize-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
